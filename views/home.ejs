<%- include("partial/header"); %>

	<h1>Home</h1>

	<p><%=homeStartingContent%></p>

	<% posts.forEach(post => { %>
		<h1> <%= post.title %></h1>	

		<p> <%= post.status.substring(0,100) + " ..." %><a href="/post/<%= post.title%>"><small>see more</small></a> 
		</p>
	<%	}) %>

<%- include("partial/footer"); %>